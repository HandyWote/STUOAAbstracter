services:
  oap:
    build: .
    container_name: oap
    restart: always
    volumes:
      - ./events:/app/events
      - ./List.txt:/app/List.txt
      - ./env:/app/env:ro
    environment:
      - TZ=Asia/Shanghai
